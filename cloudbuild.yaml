steps:
  - id: "build image"
    name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "${_ARTIFACT_REGISTRY_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${REPO_NAME}-${BRANCH_NAME}",
        ".",
      ]

  - id: "push image"
    name: "gcr.io/cloud-builders/docker"
    args:
      [
        "push",
        "${_ARTIFACT_REGISTRY_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${REPO_NAME}-${BRANCH_NAME}",
      ]

  - id: "deploy prod service"
    name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      [
        "run",
        "deploy",
        "${REPO_NAME}-${BRANCH_NAME}",
        "--platform",
        "managed",
        "--region",
        "${_CLOUD_RUN_REGION}",
        "--allow-unauthenticated",
        "--image",
        "${_ARTIFACT_REGISTRY_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${REPO_NAME}-${BRANCH_NAME}",
      ]

  - id: "ensure prod service live"
    name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      [
        "run",
        "services",
        "update-traffic",
        "${REPO_NAME}-${BRANCH_NAME}",
        "--to-latest",
        "--platform",
        "managed",
        "--region",
        "${_CLOUD_RUN_REGION}",
      ]

substitutions:
  _ARTIFACT_REGISTRY_REGION: asia-southeast1
  _REPOSITORY: docker-images-yong-wei-portfolio
  _CLOUD_RUN_REGION: asia-southeast1
  _CLOUD_FUNCTION_REGION: asia-southeast2

timeout: 900s
